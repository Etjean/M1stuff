---
title: "CC 2 : clustering, hclust et k-means"
author: "Etienne JEAN"
date: "19 octobre 2017"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


#Introduction
Après avoir déterminé les descripteurs les plus intéressants pour prédire les poches druggables et les poches non druggables des protéines, nous tentons d'effectuer des regroupements de notre jeu de données, dit *clustering*, pour rassembler les poches qui présentent des caractéristiques semblables.

Nous utiliserons 2 méthodes de clustering : 
#Clustering hiérarchique
calcul de la distance entre les individus
plot du dendrogramme











This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars, echo=FALSE}
summary(cars)
```

#Clustering par k-means

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
